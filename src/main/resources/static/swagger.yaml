openapi: 3.0.3
info:
  title: "API de Todo Cine"
  description: "API gestionada con Spring Boot, seguridad JWT y catálogo de películas con manejo de excepciones."
  version: "1.0.0"

servers:
  - url: "http://localhost:8080/api"
    description: "Servidor Local"

security:
  - BearerAuth: []

paths:
  /usuarios:
    get:
      summary: "Buscar usuarios por nombre"
      tags: ["Usuarios"]
      security:
        - BearerAuth: []
      parameters:
        - name: "username"
          description: "Nombre de usuario para la búsqueda"
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: "OK - Lista de usuarios encontrada."
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UsuarioDTO'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      summary: "Insertar un nuevo usuario"
      tags: ["Usuarios"]
      security:
        - BearerAuth: []
      requestBody:
        description: "Datos del nuevo usuario"
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsuarioDTO'
      responses:
        '201':
          description: "Created - Usuario registrado con éxito."
        '400':
          $ref: '#/components/responses/BadRequest'

  /usuarios/{id}:
    parameters:
      - name: "id"
        description: "ID único del usuario"
        in: path
        required: true
        schema:
          type: integer
          format: int64
    get:
      summary: "Obtener usuario por ID"
      tags: ["Usuarios"]
      security:
        - BearerAuth: []
      responses:
        '200':
          description: "OK - Usuario encontrado."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsuarioDTO'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      summary: "Actualizar usuario"
      tags: ["Usuarios"]
      security:
        - BearerAuth: []
      requestBody:
        description: "Nuevos datos del usuario"
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsuarioDTO'
      responses:
        '200':
          description: "OK - Usuario actualizado."
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /usuarios/{userId}/movies:
    get:
      summary: "Obtener películas del usuario (Paginado)"
      tags: ["Películas"]
      security:
        - BearerAuth: []
      parameters:
        - name: "userId"
          description: "ID del usuario"
          in: path
          required: true
          schema:
            type: integer
            format: int64
        - name: "vista"
          description: "Filtrar por películas vistas (true/false)"
          in: query
          required: true
          schema:
            type: string
        - name: "votada"
          description: "Filtrar por películas votadas"
          in: query
          required: true
          schema:
            type: string
        - name: "orderBy"
          description: "Campo de ordenación"
          in: query
          required: true
          schema:
            type: string
        - name: "page"
          description: "Número de página"
          in: query
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: "OK - Listado paginado obtenido."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatorMovieDetailDTO'
        '403':
          $ref: '#/components/responses/Forbidden'

  /usuarios/{userId}/movies/{movieId}:
    parameters:
      - name: "userId"
        description: "ID del usuario"
        in: path
        required: true
        schema:
          type: integer
          format: int64
      - name: "movieId"
        description: "ID de la película"
        in: path
        required: true
        schema:
          type: integer
          format: int64
    put:
      summary: "Actualizar estado de una película para un usuario"
      tags: ["Películas del Usuario"]
      security:
        - BearerAuth: []
      requestBody:
        description: "Datos de la relación usuario-película"
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsuarioMovieDTO'
      responses:
        '200':
          description: "OK - Estado de la película actualizado."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovieDetailDTO'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '502':
          description: "Error en el servicio externo."

  /movies:
    get:
      summary: "Listado de películas con filtros (Paginado)"
      tags: ["Películas (General)"]
      security:
        - BearerAuth: []
      parameters:
        - name: "name"
          description: "Nombre de la película"
          in: query
          required: true
          schema: { type: string }
        - name: "status"
          description: "Estado (Released, Post-Production...)"
          in: query
          required: true
          schema: { type: string }
        - name: "region"
          description: "Región (ES, US...)"
          in: query
          required: true
          schema: { type: string }
        - name: "page"
          description: "Número de página"
          in: query
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: "Lista de películas filtrada."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatorMovieDTO'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '502':
          description: "Error en el servicio externo."

  /movies/{id}:
    get:
      summary: "Obtener detalle completo de una película por ID"
      tags: ["Películas (General)"]
      security:
        - BearerAuth: []
      parameters:
        - name: "id"
          description: "ID de la película"
          in: path
          required: true
          schema: { type: integer, format: int64 }
      responses:
        '200':
          description: "Detalle de la película."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovieDetailDTO'
        '404':
          $ref: '#/components/responses/NotFound'
        '502':
          description: "Error en servidor externo."

  /ganadores:
    get:
      summary: "Obtener ganadores por premio y año"
      tags: ["Ganadores"]
      security:
        - BearerAuth: []
      parameters:
        - name: "premioId"
          description: "ID del premio"
          in: query
          required: true
          schema: { type: integer, format: int64 }
        - name: "anyo"
          description: "Año del premio"
          in: query
          required: true
          schema: { type: integer }
        - name: "pagina"
          description: "Página de resultados"
          in: query
          required: true
          schema: { type: integer }
      responses:
        '200':
          description: "Listado de ganadores obtenido."
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatorGanadorDTO'
    post:
      summary: "Insertar un nuevo ganador"
      description: "Requiere rol de ADMIN."
      tags: ["Ganadores"]
      security:
        - BearerAuth: []
      requestBody:
        description: "Datos del ganador"
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GanadorDTO'
      responses:
        '201':
          description: "Ganador registrado."
        '403':
          description: "Permisos insuficientes."

  /premios/{id}/categorias:
    get:
      summary: "Obtener categorías de un premio"
      description: "Requiere rol de ADMIN."
      tags: ["Premios y Categorías"]
      security:
        - BearerAuth: []
      parameters:
        - name: "id"
          description: "ID del premio"
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: "Lista de categorías."
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CategoriaDTO'

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    BadRequest:
      description: "Datos inválidos."
    Unauthorized:
      description: "Token no válido."
    Forbidden:
      description: "Acceso denegado."
    NotFound:
      description: "No encontrado."

  schemas:
    UsuarioDTO:
      type: object
      required: [username, password]
      properties:
        id: { type: integer, format: int64 }
        username: { type: string }
        password: { type: string, format: password }
        rol: { type: string }

    PaginatorMovieDetailDTO:
      type: object
      properties:
        page: { type: integer }
        results:
          type: array
          items: { $ref: '#/components/schemas/MovieDetailDTO' }
        total_pages: { type: integer }
        total_results: { type: integer }

    MovieDetailDTO:
      type: object
      properties:
        id: { type: integer, format: int64 }
        original_title: { type: string }
        title: { type: string }
        poster_path: { type: string }
        overview: { type: string }
        release_date: { type: string }
        popularity: { type: number, format: double }
        vote_count: { type: integer }
        vote_average: { type: number, format: double }
        original_language: { type: string }
        favoritos: { type: boolean }
        voto: { type: number, format: double }
        vista: { type: boolean }
        total_votos_TC: { type: integer }
        votos_media_TC: { type: number, format: double }
        genres:
          type: array
          items: { $ref: '#/components/schemas/GenreDTO' }
        videos:
          type: array
          items: { $ref: '#/components/schemas/VideoDTO' }

    GenreDTO:
      type: object
      required: [id]
      properties:
        id: { type: string, description: "ID del género" }
        name: { type: string, description: "Nombre del género" }

    VideoDTO:
      type: object
      required: [id]
      properties:
        id: { type: string }
        name: { type: string }
        key: { type: string }
        site: { type: string }
        type: { type: string }

    UsuarioMovieDTO:
      type: object
      required: [usuarioId, movieId, favoritos, vista]
      properties:
        usuarioId: { type: integer, format: int64, description: "ID usuario" }
        movieId: { type: integer, format: int64, description: "ID película" }
        favoritos: { type: boolean }
        vista: { type: boolean }
        voto: { type: number, format: double, nullable: true }

    PaginatorMovieDTO:
      type: object
      properties:
        page: { type: integer }
        results:
          type: array
          items: { $ref: '#/components/schemas/MovieDTO' }
        total_pages: { type: integer }
        total_results: { type: integer }

    MovieDTO:
      type: object
      required: [id, title]
      properties:
        id: { type: integer, format: int64 }
        original_title: { type: string }
        title: { type: string }
        poster_path: { type: string }
        overview: { type: string }
        release_date: { type: string }
        popularity: { type: number, format: double }
        vote_count: { type: integer }
        vote_average: { type: number, format: double }
        original_language: { type: string }
        genres:
          type: array
          items: { $ref: '#/components/schemas/GenreDTO' }
        videos:
          type: array
          items: { $ref: '#/components/schemas/VideoDTO' }
        total_votos_TC: { type: integer }
        votos_media_TC: { type: number, format: double }

    PaginatorGanadorDTO:
      type: object
      properties:
        page: { type: integer }
        results:
          type: array
          items: { $ref: '#/components/schemas/GanadorDTO' }
        total_pages: { type: integer }
        total_results: { type: integer }

    GanadorDTO:
      type: object
      required: [premioId, categoriaId, anyo, movieId]
      properties:
        premioId: { type: integer, format: int64 }
        premio: { type: string }
        categoriaId: { type: integer, format: int64 }
        categoria: { type: string }
        anyo: { type: integer }
        movieId: { type: integer, format: int64 }
        original_title: { type: string }
        title: { type: string }
        poster_path: { type: string }
        overview: { type: string }
        release_date: { type: string }
        popularity: { type: number, format: double }
        vote_count: { type: integer }
        vote_average: { type: number, format: double }
        original_language: { type: string }
        total_votos_TC: { type: integer }
        votos_media_TC: { type: number, format: double }
        genres:
          type: array
          items: { $ref: '#/components/schemas/GenreDTO' }
        videos:
          type: array
          items: { $ref: '#/components/schemas/VideoDTO' }

    CategoriaDTO:
      type: object
      properties:
        id: { type: integer, format: int64, description: "ID categoría" }
        nombre: { type: string, description: "Nombre categoría" }